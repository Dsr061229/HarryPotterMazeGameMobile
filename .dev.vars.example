# 本地 Worker 开发用密钥模板。
# 复制为 .dev.vars（已被 .gitignore 忽略，不会进 git），填入真实值后运行 `npx wrangler dev`。
# 生产环境不要用这个文件，生产用 `npx wrangler secret put <名字>` 把密钥加密存到 Cloudflare。

# Supabase 的 service_role key（Project Settings → API → service_role）。不要用 anon key。
SUPABASE_SERVICE_KEY="在这里填 service_role key"

# 签发用户/管理员 token 的随机长字符串（建议 32 位以上随机字符）。
ADMIN_TOKEN_SECRET="在这里填一长串随机字符"

# 管理员 Dsr 的登录明文密码（Worker 用明文比对）。
ADMIN_PASSWORD="在这里填管理员密码"
